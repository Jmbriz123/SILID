# SILID
Work in Progress: Build a containerized weather data platform that ingests Philippine weather data hourly, validates and transforms it through Bronze/Silver/Gold layers, computes weather-derived productivity scores using dbt, orchestrates the pipeline with Airflow, and serves the results through a Streamlit dashboard. 
